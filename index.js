import React from 'react';
import { Text, AppRegistry } from 'react-native';

const App = () => (
        <Text>Some Text</Text>
    );

ReactNative.AppRegistry.registerComponent('latihan1',() =>App);